{
  "name": "temp-date-service",
  "version": "1.0.0",
  "description": "Microservicio para clima y fecha",
  "main": "index.js",
  "type": "module",
  "scripts": {
  "start": "node index.js",
  "lint": "echo 'No lint configured' && exit 0", 
  "build": "echo 'No build step needed' && exit 0",
  "test": "echo 'No unit tests configured' && exit 0",
  "start:test": "node index.js",
  "test:e2e": "echo 'No E2E tests configured' && exit 0"
}
,
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
